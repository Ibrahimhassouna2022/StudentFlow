import TaskBoard from "../../components/tasks/TaskBoard";

// Tasks page: renders the main task board with filters, search, and action to add tasks
export default function Page() {
  return <TaskBoard />;
}
